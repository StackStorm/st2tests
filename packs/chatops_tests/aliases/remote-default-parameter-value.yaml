---
name: remote_default_parameter_value
description: Execute a command on a remote host via SSH - default parameter value.
pack: tests
action_ref: core.remote
formats:
  - "default run {{cmd}} {{hosts=localhost}}"
